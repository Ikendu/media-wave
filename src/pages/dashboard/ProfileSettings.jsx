import SettingsContainer from '../../components/dashboard/settings/SettingsContainer'

const ProfileSettings = () => {
  return <SettingsContainer setting="profile"/>
}

export default ProfileSettings
